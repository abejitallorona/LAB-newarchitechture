// server/services/socket.service.server.js

const initSocketInstance = (httpServer) => {
  // Aquí se implementaría la lógica de sockets si fuese necesaria.
  console.log("Socket service initialized (not implemented).");
};

module.exports = {
  initSocketInstance
};
